version: "3.8"

services:

  mysql:
    image: mysql:8.0
    container_name: egx_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: egx_root_pass
      MYSQL_DATABASE: egxnexus
    ports:
      - "3306:3306"
    volumes:
      - ./services/data-store/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - mysql_/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-pegx_root_pass"]
      interval: 10s
      timeout: 5s
      retries: 5

  timescale:
    image: timescale/timescaledb:latest-pg14
    container_name: egx_timescale
    restart: always
    environment:
      POSTGRES_PASSWORD: egx_pg_pass
    ports:
      - "5432:5432"
    volumes:
      - ./services/data-store/timescale/init.sql:/docker-entrypoint-initdb.d/init.sql
      - timescale_/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    container_name: egx_redis
    restart: always
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  core-brain:
    build:
      context: ./services/core-brain
      target: development
    container_name: egx_core_brain
    restart: always
    ports:
      - "3000:3000"
    environment:
      - REDIS_URL=redis://redis:6379
      - MYSQL_URL=mysql://root:egx_root_pass@mysql:3306/egxnexus
      - ANALYSIS_ENGINE_URL=http://analysis-engine:3002
      - PREDICTION_ENGINE_URL=http://prediction-engine:3003
      - DATA_HARVESTER_URL=http://data-harvester:3005
      - PORT=3000
    depends_on:
      redis:
        condition: service_healthy
      mysql:
        condition: service_healthy
    volumes:
      - ./services/core-brain:/app
      - /app/node_modules

  auth-gateway:
    build:
      context: ./services/auth-gateway
      target: development
    container_name: egx_auth_gateway
    restart: always
    ports:
      - "3001:3001"
    environment:
      - REDIS_URL=redis://redis:6379
      - PORT=3001
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./services/auth-gateway:/app
      - /app/node_modules

  analysis-engine:
    build:
      context: ./services/analysis-engine
      target: development
    container_name: egx_analysis_engine
    restart: always
    ports:
      - "3002:3002"
    environment:
      - REDIS_URL=redis://redis:6379
      - MYSQL_URL=mysql://root:egx_root_pass@mysql:3306/egxnexus
      - PORT=3002
    depends_on:
      redis:
        condition: service_healthy
      mysql:
        condition: service_healthy
    volumes:
      - ./services/analysis-engine:/app
      - /app/node_modules

  prediction-engine:
    build:
      context: ./services/prediction-engine
      target: development
    container_name: egx_prediction_engine
    restart: always
    ports:
      - "3003:3003"
    environment:
      - REDIS_URL=redis://redis:6379
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - BROKER_ID=thndr
      - PROCESS_MODE=listener
      - TV_USERNAME=your_thndr_email
      - TV_PASSWORD=your_thndr_password
      - HEADLESS_MODE=true
      - AUTH_REFRESH_HOURS=12
      - PORT=3003
    depends_on:
      redis:
        condition: service_healthy
      timescale:
        condition: service_healthy
    volumes:
      - ./services/prediction-engine:/app
      - /app/node_modules

  data-harvester:
    build:
      context: ./services/data-harvester
    container_name: egx_data_harvester
    restart: always
    ports:
      - "3005:3005"
    environment:
      - MYSQL_URL=mysql://root:egx_root_pass@mysql:3306/egxnexus
      - REDIS_URL=redis://redis:6379
      - PORT=3005
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./services/data-harvester:/app
      - /app/node_modules

  notification-hub:
    build:
      context: ./services/notification-hub
      target: development
    container_name: egx_notification_hub
    restart: always
    ports:
      - "3006:3006"
    environment:
      - REDIS_URL=redis://redis:6379
      - TELEGRAM_BOT_TOKEN=your_telegram_token
      - TELEGRAM_CHAT_ID=your_chat_id
      - PORT=3006
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./services/notification-hub:/app
      - /app/node_modules

  dashboard-api:
    build:
      context: ./services/dashboard-api
      target: development
    container_name: egx_dashboard_api
    restart: always
    ports:
      - "3007:3007"
    environment:
      - REDIS_URL=redis://redis:6379
      - MYSQL_URL=mysql://root:egx_root_pass@mysql:3306/egxnexus
      - PORT=3007
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./services/dashboard-api:/app
      - /app/node_modules

  nginx:
    image: nginx:alpine
    container_name: egx_nginx
    ports:
      - "80:80"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - core-brain
      - auth-gateway
      - analysis-engine
      - prediction-engine
      - data-harvester
      - notification-hub
      - dashboard-api

volumes:
  mysql_
  timescale_
  redis_
