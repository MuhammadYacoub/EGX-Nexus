fastapi>=0.95.0
uvicorn>=0.21.1
pydantic>=1.10.7
redis>=4.5.4
numpy>=1.24.2
pandas>=2.0.0
# The specific deep learning frameworks will be determined by the DL_FRAMEWORK env var,
# but we add PyTorch (CPU by default for generic installs) as a placeholder.
torch>=2.0.0
